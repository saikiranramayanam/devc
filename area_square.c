#include<stdio.h>
int main()
{
	int a,b;
	scanf("%d",&a);
	b = a*a;
	b = 4*a;
	printf("area of a square with side %d is: %d\n",a,b);
	printf("perimeter of a square is %d is: %d\n",a,b);
	return 0;
}
